{
	"name": "@badger/platformer-core",
	"version": "1.0.0",
	"type": "module",
	"description": "Pure physics, collision, and ECS foundation for platformers.",
	"exports": {
		".": {
			"import": "./src/index.ts",
			"types": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"test": "vitest run --pool=forks",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@vitest/ui": "^1.0.0",
		"typescript": "^5.3.3",
		"vitest": "^1.0.0"
	}
}
