{
	"name": "@badger/codegate",
	"version": "1.0.0",
	"type": "module",
	"description": "Extractable minigame engine: code gates and six challenge kinds.",
	"exports": {
		".": {
			"import": "./src/index.ts",
			"types": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@vitest/ui": "^1.0.0",
		"typescript": "^5.3.3",
		"vitest": "^1.0.0"
	}
}
