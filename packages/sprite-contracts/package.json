{
	"name": "@badger/sprite-contracts",
	"version": "1.0.0",
	"type": "module",
	"description": "Shared sprite schema, playback, production geometry, validation, and browser loader.",
	"exports": {
		".": {
			"import": "./src/index.ts",
			"types": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@vitest/ui": "^1.0.0",
		"typescript": "^5.3.3",
		"vitest": "^1.0.0"
	}
}
