import type { SpriteManifest } from './types';
/**
 * Validate sprite manifest structure
 * Checks version string, sheets array, and each sheet's properties
 */
export declare function validateSpriteManifest(manifest: unknown): manifest is SpriteManifest;
//# sourceMappingURL=validate.d.ts.map